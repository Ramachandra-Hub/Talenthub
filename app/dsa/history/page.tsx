import { DsaHistoryView } from '@/components/dsa/dsa-history-view';

export const metadata = { title: 'DSA history — RCE' };

export default function DsaHistoryPage() {
  return <DsaHistoryView />;
}
