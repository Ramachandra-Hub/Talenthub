import { DsaArena } from '@/components/dsa/arena/dsa-arena';

export const metadata = {
  title: 'DSA Arena — ELEVATE-X',
};

/** DSA Arena tab = gamified Adventure Map (coding missions open Code Lab). */
export default function DsaArenaHomePage() {
  return <DsaArena />;
}
